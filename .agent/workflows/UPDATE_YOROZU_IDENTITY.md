---
description: リポジトリを「YOROZU」ブランド（雅・和風）に更新し、READMEレイアウトを整備します
---
# YOROZU アイデンティティ更新

このワークフローは、リポジトリのアイデンティティを「YOROZU」スタイル（雅な和風デザイン）に統一し、READMEのレイアウトを標準化します。

## 手順1: 🎨 ヘッダー画像生成（雅） // 高速
- `/generate-header-image` ワークフローを呼び出すか、同等の手順を実行します。
  - **スタイル**: 和風の雅（金箔、藍色、和柄）。
  - **テキスト**: プロンプトに **「そのリポジトリの名前（英字表記）」** を含めます（漢字・ひらがな・カタカナは禁止）。
  - 保存先: `.agent/assets/header.png`（16:9）

## 手順2: 📝 README更新
- `README.md` を編集して以下の構成にします：
  1. **ヘッダー画像**: 最上部に配置。
  2. **タイトル**: `# YOROZU`
  3. **サブタイトル**: `### ～あなた専用のリポジトリ整理・零重力ユーティリティ～`
  4. **バッジ**: 既存のバッジを維持。
  5. **YOROZU注記**: 以下のアラートを概要の直後または末尾に追加します。
     ```markdown
     > [!NOTE]
     > このリポジトリはYOROZUで作成されました
     > https://github.com/Sunwood-ai-labs/YOROZU
     ```

## 手順3: ✅ 検証
- 生成された画像が「雅」であり、テキストが正しく含まれているか確認します。
- READMEのプレビューを確認し、レイアウトが崩れていないかチェックします。
